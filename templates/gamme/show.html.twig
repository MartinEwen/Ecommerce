{% extends 'admin/index.html.twig' %}

{% block title %}Gamme
{% endblock %}

{% block admin %}
	<h1>Gamme</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ gamme.id }}</td>
			</tr>
			<tr>
				<th>NameGamme</th>
				<td>{{ gamme.nameGamme }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_gamme_index') }}">back to list</a>

	<a href="{{ path('app_gamme_edit', {'id': gamme.id}) }}">edit</a>

	{{ include('gamme/_delete_form.html.twig') }}
{% endblock %}
